import {DatasourceModel} from '../datasourceModel';

export class MeshNodesDatasourceModel extends DatasourceModel {
  address!: number;
  zone!: string;
  anchorNode!: boolean;
  location!: string;
}
